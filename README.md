# aws-apprunner

# Flask App Runner Backend

## Local run
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
python -m pip install -r requirements.txt
python app.py

## Endpoints
GET  /health
POST /echo
